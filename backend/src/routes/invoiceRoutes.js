// import express from "express";
// import { getAllInvoices, getResidentInvoices } from "../controllers/invoiceController.js";
// import { auth, isAdmin, isResident } from "../middleware/role.js";

// const router = express.Router();

// router.get("/admin", auth, isAdmin, getAllInvoices);
// router.get("/resident", auth, isResident, getResidentInvoices);

// export default router;
